import React from "react";

const HorizontailBar = ({ text }) => {
    return (
        <div className="horizontal-bar">
            <h3>{text}</h3>
        </div>
    );
};

export default HorizontailBar;
